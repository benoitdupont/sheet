package be.dupont;

import org.junit.Test;

import java.util.Collections;

import static be.dupont.SheetTest.BASIC_SHEET;
import static be.dupont.SheetTest.part;
import static java.util.Arrays.asList;
import static org.junit.Assert.assertEquals;

public class Randomizer {

    @Test
    public void emptySheet() {
        // Get random list of parts with an area ~= sheet area, (minus some parts if too difficult)

        // Get rid of the invalid ones

        // Compute the space left for each and choose the best solutions

    }
}
